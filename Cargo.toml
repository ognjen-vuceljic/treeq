[package]
name = "treeq"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
roxmltree = "0.20"
clap = { version = "4", features = ["derive"] }
